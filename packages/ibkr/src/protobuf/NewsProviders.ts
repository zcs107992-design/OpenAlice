// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: NewsProviders.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { NewsProvider } from "./NewsProvider";

export const protobufPackage = "protobuf";

export interface NewsProviders {
  newsProviders: NewsProvider[];
}

function createBaseNewsProviders(): NewsProviders {
  return { newsProviders: [] };
}

export const NewsProviders: MessageFns<NewsProviders> = {
  encode(message: NewsProviders, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.newsProviders) {
      NewsProvider.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewsProviders {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNewsProviders();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newsProviders.push(NewsProvider.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NewsProviders {
    return {
      newsProviders: globalThis.Array.isArray(object?.newsProviders)
        ? object.newsProviders.map((e: any) => NewsProvider.fromJSON(e))
        : [],
    };
  },

  toJSON(message: NewsProviders): unknown {
    const obj: any = {};
    if (message.newsProviders?.length) {
      obj.newsProviders = message.newsProviders.map((e) => NewsProvider.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<NewsProviders>): NewsProviders {
    return NewsProviders.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NewsProviders>): NewsProviders {
    const message = createBaseNewsProviders();
    message.newsProviders = object.newsProviders?.map((e) => NewsProvider.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
