// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: HistoricalTick.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "protobuf";

export interface HistoricalTick {
  time?: number | undefined;
  price?: number | undefined;
  size?: string | undefined;
}

function createBaseHistoricalTick(): HistoricalTick {
  return { time: undefined, price: undefined, size: undefined };
}

export const HistoricalTick: MessageFns<HistoricalTick> = {
  encode(message: HistoricalTick, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.time !== undefined) {
      writer.uint32(8).int64(message.time);
    }
    if (message.price !== undefined) {
      writer.uint32(17).double(message.price);
    }
    if (message.size !== undefined) {
      writer.uint32(26).string(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HistoricalTick {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistoricalTick();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.time = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.price = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.size = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistoricalTick {
    return {
      time: isSet(object.time) ? globalThis.Number(object.time) : undefined,
      price: isSet(object.price) ? globalThis.Number(object.price) : undefined,
      size: isSet(object.size) ? globalThis.String(object.size) : undefined,
    };
  },

  toJSON(message: HistoricalTick): unknown {
    const obj: any = {};
    if (message.time !== undefined) {
      obj.time = Math.round(message.time);
    }
    if (message.price !== undefined) {
      obj.price = message.price;
    }
    if (message.size !== undefined) {
      obj.size = message.size;
    }
    return obj;
  },

  create(base?: DeepPartial<HistoricalTick>): HistoricalTick {
    return HistoricalTick.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HistoricalTick>): HistoricalTick {
    const message = createBaseHistoricalTick();
    message.time = object.time ?? undefined;
    message.price = object.price ?? undefined;
    message.size = object.size ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
