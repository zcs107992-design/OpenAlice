// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: HistogramData.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { HistogramDataEntry } from "./HistogramDataEntry";

export const protobufPackage = "protobuf";

export interface HistogramData {
  reqId?: number | undefined;
  histogramDataEntries: HistogramDataEntry[];
}

function createBaseHistogramData(): HistogramData {
  return { reqId: undefined, histogramDataEntries: [] };
}

export const HistogramData: MessageFns<HistogramData> = {
  encode(message: HistogramData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reqId !== undefined) {
      writer.uint32(8).int32(message.reqId);
    }
    for (const v of message.histogramDataEntries) {
      HistogramDataEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HistogramData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistogramData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reqId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.histogramDataEntries.push(HistogramDataEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistogramData {
    return {
      reqId: isSet(object.reqId) ? globalThis.Number(object.reqId) : undefined,
      histogramDataEntries: globalThis.Array.isArray(object?.histogramDataEntries)
        ? object.histogramDataEntries.map((e: any) => HistogramDataEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: HistogramData): unknown {
    const obj: any = {};
    if (message.reqId !== undefined) {
      obj.reqId = Math.round(message.reqId);
    }
    if (message.histogramDataEntries?.length) {
      obj.histogramDataEntries = message.histogramDataEntries.map((e) => HistogramDataEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<HistogramData>): HistogramData {
    return HistogramData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HistogramData>): HistogramData {
    const message = createBaseHistogramData();
    message.reqId = object.reqId ?? undefined;
    message.histogramDataEntries = object.histogramDataEntries?.map((e) => HistogramDataEntry.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
